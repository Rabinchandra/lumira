-- Sample data. Requires that auth.users already contains the referenced UUIDs.
-- Use Supabase dashboard to create test users first, then replace the
-- placeholder UUIDs below with their ids.

-- Example shape only — uncomment and edit before running.
-- insert into public.profiles (id, display_name, phone) values
--   ('00000000-0000-0000-0000-000000000001', 'Aanya Mehra', '+91 90000 00001');

-- insert into public.teams (id, name, initials, color, invite_code, owner_id) values
--   ('11111111-1111-1111-1111-111111111111', 'Lumière Co.', 'LC', '#7C5CFC', 'LUM4X9',
--    '00000000-0000-0000-0000-000000000001');
